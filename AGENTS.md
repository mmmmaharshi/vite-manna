# Git Workflow

- Feature branches for all major work: `feat/<brief-description>`
- PR into `dev` for integration
- PR from `dev` into `main` for production releases
- No direct commits to `dev` or `main` for feature work
- Always run `lint` and `test` before committing


# Testing instructions
- After feature implemented test aginist relevent `agent skills` aginist it only after passing commit it

